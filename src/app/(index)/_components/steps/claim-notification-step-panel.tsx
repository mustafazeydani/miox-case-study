"use client";

import {
  ClaimStepPanelFrame,
  type ClaimStepPanelProps,
} from "../claim-step-panel-frame";

export default function ClaimNotificationStepPanel(props: ClaimStepPanelProps) {
  return <ClaimStepPanelFrame {...props} />;
}
