"use client";

import {
  ClaimStepPanelFrame,
  type ClaimStepPanelProps,
} from "../claim-step-panel-frame";

export default function AppraisalStepPanel(props: ClaimStepPanelProps) {
  return <ClaimStepPanelFrame {...props} />;
}
